import React, { PureComponent } from 'react';
import './index.scss';

class <%= name %> extends PureComponent {
  render() {
    return (
      <div>a new react component</div>
    );
  }
}

export default <%= name %>;

